# API keys (contains secrets)
.env
# Note: .mcp.json is committed — it only has ${ENV_VAR} placeholders, no actual secrets

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
*.code-workspace

# Node modules (if MCPs install locally)
node_modules/

# Python
__pycache__/
*.pyc
.venv/
venv/
